<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.PicturesViewer" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:statusBarColor">#3F51B5</item>
    </style>

    <style name="Theme.PicturesViewer" parent="Base.Theme.PicturesViewer" />
</resources>